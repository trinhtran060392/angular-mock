{
  "version": "0.0.0",
  "private": true,
  "name": "angular-cfn-mock",
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 9002 -c-1"
  },
  "dependencies": {
    "bootstrap": "~3.3.6"
  }
}
