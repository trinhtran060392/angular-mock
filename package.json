{
  "version": "0.0.0",
  "private": true,
  "name": "angular-cfn-mock",
  "devDependencies": {
    "bower": "^1.3.1",
    "lite-server": "^2.2.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "lite-server"
  },
  "dependencies": {
    "bootstrap": "~3.3.6"
  }
}
